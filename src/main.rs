use node::*;

fn main() {
    // let mut state = State::Follower(Follower {
    //     term: 0,
    //     voted_for: None,
    //     leader: None,
    // });
    // state = state
    //     .process_message(state::Message::Timeout)
    //     .expect("Failed to process message")
    //     .0;

    // println!("{:?}", state);
}
